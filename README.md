Programming Goals
2026 - 2027

<b>HTML, CSS, JS</b> -> functional, responsive web-design with layout scripting for web browsers 
<s>React & Typescript</s>

<b>Troubleshooting Layout problems</b> ->


<s>Fast UI development</s>

<b>Python</b> -> re-learning python
<s> Sending data from place to place over a network</s> 


Technical Goals 
2026 - 2028

Blender

Shure Pro Level 1 - 3
  
Dante Level One
  
Wireshark

Q:
how is the the web browser reading layout and style script?

Each website is a directory with files that is interpreted in real time by the web browser.

Just as one would expect a .word file or a .txt file to work, .html, .css and .js files add content from the top, left to right. 

New content pushes old content down, until the newest content is on the top of the stack in memory. 

HTML is a layout code. CSS is style code. They are interpreted by the web browser as they are written. 
Web browsers come with default behavior. By default, the browser interprets HTML as Block level code.

In html, some html tags are Block level.They <i>contain</I> the content. They take up 100 percent of the width of available web browser space. 
A coder specifies their height with css and the web browser will assign and re-assign that at the frame rate computer refreshes at. 

Inline level html tags just <I>wrap</i> content, which is variable. Content could be a film, a poem, anything. It takes rules from its parent.In css, one sets the rules to the parent it lives in or to the child-item itself. 
See how the words <i> contain </I>and <I>wrap</i> are <I>italicized</I>? Inline level code.
<div> See how this takes up the entire line>?</div>, its wrapped in block level html unlike this line here. 

<!---
trncm00/trncm00 is a ✨ special ✨ repository because its `README.md` (this file) appears on your GitHub profile.
You can click the Preview link to take a look at your changes.
--->
